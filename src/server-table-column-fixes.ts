export function initServerTableColumnFixes() {
  // La tabla de servidores ya se renderiza con sus columnas finales desde React.
  // Este archivo queda solo para compatibilidad con importaciones existentes.
}
